apiVersion: v1
kind: Service
metadata:
  name: mess-backend
  namespace: mess-system
spec:
  selector:
    app: mess-backend
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
  type: ClusterIP
